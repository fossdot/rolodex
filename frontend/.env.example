# PocketBase URL the browser connects to.
# Read at RUNTIME by the Node server ($env/dynamic/public) — in production,
# set this in the systemd unit (deploy/sveltekit.service); no rebuild needed.
# Locally you can omit it; it falls back to http://127.0.0.1:8090.
PUBLIC_PB_URL=http://127.0.0.1:8090
